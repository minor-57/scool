<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="../css/normalize.css">
    <link rel="stylesheet" href="../css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/catalog.css">
    <link rel="icon" type="image/png" href="../../img/main-icon.png">
    <title>{{data.ex}} - {{data.title}}</title>
</head>
<body>
    <div class="wrapper">
        <div class="body-content">
            <section class="body-content__section main-section">
                <div class="side-section">
                    <div class="side-section__button-back"></div>
                    <div class="side-section__main-text">
                        <div class="menu-logo">
                            <a href="../index.html" class="logo-olimpys"></a>
                        </div>
                        <ul class="side-section__list">
                            <li class="side-section__item"><a href="{{data.lVideo}}" class="side-section__link active">Видеоуроки</a>
                            </li>
                            <li class="side-section__item"><a
                                    href="{{data.lTasks}}"
                                    class="side-section__link">Каталог заданий</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </section>
        </div>



        <div class="container-fluid container-limit">
            <div class="row">
                <div class="col-xxl-3 col-xl-4 col-lg-6 col-md-12">
                    <span class="subject-name"><span class-"exam">{{data.ex}}</span> - <span class="subj">{{data.title}}</span></span>
                    <h1 class="catalog-heading">Видеоуроки</h1>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-12">
                    <div class="catalog-info">
                        <h3 class="catalog-info__heding">Бесплатный каталог уроков по {{data.subject}}.</h3>
                        <p class="catalog-info__description">Здесь вы можете ознакомиться с решением большинства типовых
                            задач разной сложности.</p>
                    </div>
                </div>
            </div>
            {% if data.folders | length > 0 %}
                <div class="row justify-content-evenly folders-row">
                    {% for item in data.folders %}
                        <div class="col-xxl-3 col-xl-4 col-lg-6 col-md-12 folder-cols">
                            <a href="{{item.link}}">
                                <div class="folder-card">
                                    <img src="../img/folder.png" alt="folder" class="folder-img">
                                    <strong class="folder-name">{{item.name}}</strong>
                                </div>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
            {% if videos %}
                <div class="row justify-content-evenly folders-row">
                {% for video in videos %}
                    <iframe class="col-xxl-3 col-xl-4 col-lg-6 col-md-12 player"
                    src="{{video.src}}"
                    title="{{video.title}}" frameborder="0"
                    allow="accelerometer; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                    allowfullscreen></iframe>
                {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
    <script src="../js/catalog.js"></script>
</body>

</html>